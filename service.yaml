apiVersion: v1
kind: Service

metadata:
  name: express-service
  namespace: dev

spec:

  selector:
    app: express

  type: NodePort

  ports:

    - port: 3000

      targetPort: 3000

      nodePort: 30080